{
  "name": "path-open",
  "main": "./lib/path-open",
  "version": "0.0.0",
  "description": "Opens relative paths under the cursor using `Path: Open` from the Command Palette, `Open Path` from the context menu, or `ctrl-alt-p`.",
  "keywords": [
  ],
  "activationCommands": {
    "atom-workspace": "path:open"
  },
  "repository": "https://github.com/thisishuey/path-open",
  "license": "MIT",
  "engines": {
    "atom": ">=1.0.0 <2.0.0"
  },
  "dependencies": {
    "first-mate": "^3.0.0"
  },
  "devDependencies": {
    "coffeelint": "^1.9.7"
  }
}
